---
const features = [
  {
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7z" stroke="currentColor" stroke-width="1.5"/><circle cx="12" cy="9" r="2.5" stroke="currentColor" stroke-width="1.5"/></svg>`,
    title: 'Google Maps Extraction',
    description: 'Pull business data from any location or category. Names, addresses, phones, websites, and ratings - all in one scrape.',
    highlight: 'Any niche, any city'
  },
  {
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none"><circle cx="12" cy="8" r="4" stroke="currentColor" stroke-width="1.5"/><path d="M4 20c0-4 4-6 8-6s8 2 8 6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/><path d="M16 11l2 2 3-3" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>`,
    title: 'Decision-Maker Emails',
    description: "We find the owner or manager, not generic contacts. Skip the gatekeepers and reach the people who make decisions.",
    highlight: '40% success rate'
  },
  {
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none"><path d="M9 12l2 2 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/><path d="M12 3l8.66 5v8.66L12 22l-8.66-5.34V8L12 3z" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round"/></svg>`,
    title: 'Email Verification',
    description: 'Every email passes through SMTP validation, catch-all detection, and deliverability scoring. Only verified emails make the cut.',
    highlight: '90%+ deliverability'
  },
  {
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none"><rect x="3" y="3" width="18" height="18" rx="2" stroke="currentColor" stroke-width="1.5"/><path d="M7 8h10M7 12h10M7 16h6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>`,
    title: 'Flexible Export',
    description: 'Download as CSV or push directly to HubSpot, Salesforce, Pipedrive, or any CRM that accepts imports.',
    highlight: 'CSV + CRM integrations'
  },
  {
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="3" stroke="currentColor" stroke-width="1.5"/><path d="M12 2v4M12 18v4M4.93 4.93l2.83 2.83M16.24 16.24l2.83 2.83M2 12h4M18 12h4M4.93 19.07l2.83-2.83M16.24 7.76l2.83-2.83" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>`,
    title: 'All-in-One Pipeline',
    description: "Stop juggling Apollo, Hunter, and Neverbounce. One platform handles scraping, enrichment, and verification.",
    highlight: 'Replace 3 tools'
  },
  {
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none"><path d="M12 8v4l3 3" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/><circle cx="12" cy="12" r="9" stroke="currentColor" stroke-width="1.5"/></svg>`,
    title: 'Fresh Data Only',
    description: "No stale databases. Every lead is scraped and enriched on-demand, so your data is always current.",
    highlight: 'Real-time scraping'
  }
];
---

<section id="features" class="features">
  <div class="container">
    <div class="section-header fade-up">
      <span class="badge">Features</span>
      <h2>Everything you need.<br /><span class="data-highlight">Nothing you don't.</span></h2>
      <p>A complete lead generation pipeline in one platform. No duct tape required.</p>
    </div>

    <div class="features-grid">
      {features.map((feature, index) => (
        <div class={`feature-card glass-card fade-up stagger-${(index % 3) + 1}`}>
          <div class="feature-icon" set:html={feature.icon} />
          <h3>{feature.title}</h3>
          <p>{feature.description}</p>
          <span class="feature-highlight mono">{feature.highlight}</span>
        </div>
      ))}
    </div>

    <div class="features-comparison fade-up">
      <div class="comparison-header">
        <h3>Why teams switch to PullLeads</h3>
      </div>
      <div class="comparison-table">
        <div class="comparison-row header">
          <span></span>
          <span>PullLeads</span>
          <span>Apollo</span>
          <span>Hunter</span>
        </div>
        <div class="comparison-row">
          <span>Google Maps scraping</span>
          <span class="yes">Yes</span>
          <span class="no">No</span>
          <span class="no">No</span>
        </div>
        <div class="comparison-row">
          <span>Owner/Decision-maker emails</span>
          <span class="yes">Yes</span>
          <span class="partial">Limited</span>
          <span class="no">Generic only</span>
        </div>
        <div class="comparison-row">
          <span>Built-in verification</span>
          <span class="yes">Yes</span>
          <span class="partial">Extra cost</span>
          <span class="partial">Basic</span>
        </div>
        <div class="comparison-row">
          <span>Fresh, on-demand data</span>
          <span class="yes">Yes</span>
          <span class="no">Database</span>
          <span class="no">Database</span>
        </div>
        <div class="comparison-row">
          <span>Starting price</span>
          <span class="highlight mono">$49/mo</span>
          <span class="mono dim">$99/mo</span>
          <span class="mono dim">$49/mo</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .features {
    position: relative;
    z-index: 2;
  }

  .section-header {
    text-align: center;
    max-width: 560px;
    margin: 0 auto 4rem;
  }

  .section-header .badge {
    margin-bottom: 1.5rem;
  }

  .section-header h2 {
    font-size: clamp(2rem, 4vw, 3rem);
    margin-bottom: 1rem;
  }

  .section-header p {
    color: var(--text-secondary);
    font-size: 1.0625rem;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
    margin-bottom: 4rem;
  }

  .feature-card {
    display: flex;
    flex-direction: column;
    gap: 0.875rem;
  }

  .feature-icon {
    width: 44px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(99, 102, 241, 0.1);
    border-radius: var(--radius-md);
    color: var(--accent-glow);
  }

  .feature-card h3 {
    font-size: 1.125rem;
    font-weight: 600;
  }

  .feature-card p {
    color: var(--text-secondary);
    font-size: 0.9rem;
    line-height: 1.6;
    flex: 1;
  }

  .feature-highlight {
    font-size: 0.7rem;
    color: var(--accent-glow);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    padding-top: 0.75rem;
    border-top: var(--border-subtle);
  }

  /* Comparison */
  .features-comparison {
    background: var(--bg-surface);
    border: var(--border-subtle);
    border-radius: var(--radius-xl);
    overflow: hidden;
  }

  .comparison-header {
    padding: 1.75rem 2rem;
    text-align: center;
    border-bottom: var(--border-subtle);
  }

  .comparison-header h3 {
    font-size: 1.25rem;
    font-weight: 600;
  }

  .comparison-table {
    padding: 1rem;
  }

  .comparison-row {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr 1fr;
    gap: 1rem;
    padding: 0.875rem 1.25rem;
    align-items: center;
    border-radius: var(--radius-md);
    transition: background var(--transition-fast);
  }

  .comparison-row:not(.header):hover {
    background: rgba(255, 255, 255, 0.02);
  }

  .comparison-row.header {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    color: var(--text-muted);
    border-bottom: var(--border-subtle);
    margin-bottom: 0.375rem;
    padding-bottom: 1rem;
  }

  .comparison-row.header span:nth-child(2) {
    color: var(--accent-glow);
    font-weight: 600;
  }

  .comparison-row span {
    text-align: center;
    font-size: 0.875rem;
  }

  .comparison-row span:first-child {
    text-align: left;
    color: var(--text-secondary);
  }

  .comparison-row .yes {
    color: var(--success);
    font-weight: 600;
  }

  .comparison-row .no {
    color: var(--text-muted);
  }

  .comparison-row .partial {
    color: var(--warning);
  }

  .comparison-row .highlight {
    color: var(--accent-glow);
    font-weight: 600;
  }

  .comparison-row .dim {
    color: var(--text-muted);
  }

  @media (max-width: 900px) {
    .features-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .comparison-row {
      grid-template-columns: 1.5fr 1fr 1fr 1fr;
      font-size: 0.85rem;
      padding: 0.75rem;
    }
  }

  @media (max-width: 600px) {
    .features-grid {
      grid-template-columns: 1fr;
    }

    .comparison-row {
      grid-template-columns: 1fr;
      gap: 0.5rem;
      padding: 1.25rem;
      border-bottom: var(--border-subtle);
    }

    .comparison-row.header {
      display: none;
    }

    .comparison-row span {
      text-align: left;
      display: flex;
      justify-content: space-between;
    }

    .comparison-row span:first-child {
      font-weight: 600;
      color: var(--text-primary);
      margin-bottom: 0.5rem;
    }
  }
</style>
